# Changelog

## 0.1.0

- 在 DSH Web 侧边栏会话列表的三点菜单底部注入「删除」项。
- 二次确认对话框，确认后硬删除会话（live 会话 + 磁盘持久化）。
- 冷会话删除后通过 `session/disposed` 广播，使所有前端列表即时移除（无需手动刷新）。
- 主题适配：直接使用官方 `--dsw-*` 主题变量，亮/暗/皮肤主题自动解析。
- 确认框按钮带 `aria-label`，便于辅助技术识别。
